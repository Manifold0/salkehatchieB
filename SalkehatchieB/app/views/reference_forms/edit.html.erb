<h1 class ="text-muted">Editing Reference Form</h1>

<%= render 'form' %>

<%= link_to 'Show', @reference_form %> |

<% if current_user.is_admin? %>
    <%= link_to 'Back', reference_forms_path %>
<% else %>
    <%= link_to 'Back', reference_forms_path(current_user.reference_forms)   %>
<% end %>
