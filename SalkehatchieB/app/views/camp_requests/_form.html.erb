<%= form_for @camp_request, :url => request_camps_create_path(@camp_request) do |f| %>

  <% if @camp_request.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@camp_request.errors.count, "error") %> prohibited this camp_request from being saved:</h2>

      <ul>
      <% @camp_request.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  

  <%

  array = Array.new()
  array.push(['none',nil]);
  Camp.all.each do |camp|
    array.push([camp.city,camp.id])
  end
%>
  
  <div class="field">
    <%= f.label :preference1 %><br>
    <%= f.select(:preference1_id, array) %>
  </div>
  

  <div class="field">
    <%= f.label :preference2 %><br>
    <%= f.select(:preference2_id, array) %>
  </div>
  <div class="field">
    <%= f.label :preference3 %><br>
   <%= f.select(:preference3_id, array) %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
